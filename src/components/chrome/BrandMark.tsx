// Pulsing dot + callsign. Used as the NavigationBar title slot.
export function BrandMark() {
  return (
    <span className="hg-brand">
      <span className="dot" />
      <span>contextual</span>
      <span className="sub">· workable prototype</span>
    </span>
  );
}
