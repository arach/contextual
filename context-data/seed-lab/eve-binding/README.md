# eve-canvas (binding geometry)

Arrow ↔ element binding math for the Eve canvas. The core is `bindingPoint` in
`geometry.mjs`, which computes where an arrow attaches to a shape's perimeter.

```
npm test
```
